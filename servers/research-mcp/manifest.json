{
  "manifest_version": "0.2",
  "name": "research-mcp",
  "version": "0.1.0",
  "description": "MCP server providing access to OpenAlex, Crossref, and Europe PMC for academic research",
  "author": {
    "name": "cluster-mcp"
  },
  "license": "MIT",
  "server": {
    "type": "node",
    "entry_point": "dist/server.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/dist/server.js"
      ],
      "env": {
        "CONTACT_EMAIL": "${user_config.contact_email}"
      }
    }
  },
  "compatibility": {
    "runtimes": {
      "node": ">=20.0.0"
    }
  },
  "user_config": {
    "contact_email": {
      "type": "string",
      "title": "Contact Email",
      "description": "Email address used in API user-agents for OpenAlex and Crossref courtesy headers.",
      "required": false
    }
  }
}
