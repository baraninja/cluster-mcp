{
  "name": "health-mcp",
  "version": "0.1.0",
  "description": "MCP server for global health indicators (WHO, OECD, World Bank)",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc && node scripts/postbuild.cjs",
    "dev": "tsc --watch",
    "start": "node dist/server.js",
    "pack:mcpb": "npm run build && node -e \"const fs=require('fs');const path=require('path');fs.mkdirSync(path.resolve(process.cwd(),'../../bundles'),{recursive:true});\" && mcpb pack . ../../bundles/health-mcp.mcpb",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@cluster-mcp/core": "*",
    "@modelcontextprotocol/sdk": "^1.18.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
